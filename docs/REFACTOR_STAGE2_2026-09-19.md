# 2단계: 중복 함수 정리 — 2026-09-19

- [x] `openTestParticipateModal`, `closePledgeModal`의 앞쪽 중복 선언 제거(170줄).
- [x] 현재 실행되는 뒤쪽 두 함수의 본문은 변경 전과 동일함을 확인.
- [x] 변경 전후 같은 동작 테스트 8개 통과: 로그인 차단, 모달 열기·닫기·재진입, 참여 화면 초기화, 등록 자격 안내.
- [x] 중복 선언 재발 방지 검사 추가.
- [x] 새 빌드의 Chrome에서 비로그인 등록·참여 클릭 → 로그인 안내 → 로그인 화면 이동 확인.
- [x] 전체 테스트 **225개 통과, 실패 0개, TODO 1개**. 빌드 및 `git diff --check` 통과.

로그인 후 모달 동작은 세션·DOM을 모의한 자동 테스트로 검증했다. 실제 계정으로 로그인한 브라우저 재검증은 이번 단계에서 하지 않았다.

기존 문제: 참여 모달의 대상 태그 표시 코드는 죽은 함수에만 있었다. 기존 테스트는 그 코드의 존재만 검사해 통과했다. 해당 검사를 실제 실행 테스트의 TODO로 옮겼으며, 변경 전 코드에서도 같은 실패를 재현했다. 기능 복구는 별도 수정으로 진행한다.

2단계의 앱 변경은 중복 선언 삭제뿐이다. 후속 [3단계 화면별 HTML 분리](REFACTOR_STAGE3_2026-09-19.md)도 완료했으며, [1단계 기준 기록](REFACTOR_BASELINE_2026-09-19.md)의 캡처와 원본 해시는 그대로 유지한다.
