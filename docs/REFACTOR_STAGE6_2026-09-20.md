# 6단계: 로그인 후 최종 동작 검증 — 2026-09-20

전체 분리 작업의 최종 확인 결과는 [7단계 기록](REFACTOR_STAGE7_2026-09-20.md)을 참고한다.

**확인한 주요 흐름에서 파일 분리로 인한 새 오류는 발견하지 않았다. 화면별 디자인 개선을 진행할 수 있다.** 신규 교환 성공과 아래 미확인 조건까지 모두 검증했다는 뜻은 아니다.

Chrome에서 최종 빌드(`127.0.0.1:4179`)와 사용자가 로그인한 계정으로 실제 저장·새로고침을 확인했다.

- [x] 로그인 유지: 새로고침 후 계정·잔액·내 프로젝트 복원.
- [x] 등록·수정: 검증용 프로젝트 등록 후 제목 수정 저장. 상세·목록·수정 화면 및 새로고침 후 제목·설명·링크·문항·모집 인원·보상 복원 확인. 서비스 카테고리는 아래 예외에 해당.
- [x] 제작자 결과 화면: 새 프로젝트의 참여·리뷰 0건 표시 확인.
- [x] 참여·재개: 외부 프로덕트 신규 참여 → 서비스 체험 → 나중에 작성 → 새로고침 → 리뷰 대기 목록에서 재개.
- [x] 후기·보상: 별점 4점, 객관식 2문항·서술형 답변 저장. 새로고침 후 공개 리뷰·완료 CTA 유지, 대기 1→0건·완료 2→3건, 잔액 490→560 C 및 원장 +70 C 확인. 화면에서 재제출 진입이 사라짐.
- [x] 상점: 기존 교환 1건·차감 1,200 C 내역 유지. 보유 560 C에서 최저 상품 1,200 C 선택 시 교환 버튼 비활성 확인.
- [ ] 신규 교환 성공 재검증: 잔액 640 C 부족으로 미실행. 분리 전 성공 기록은 기준 문서 B12에 있음.

최종 코드의 자동 검증은 [5단계](REFACTOR_STAGE5_2026-09-20.md)의 **241개 통과·실패 0개·기존 TODO 1개**, 빌드 성공 결과를 따른다. 이번 단계는 앱 코드 변경 없이 브라우저 검증과 문서 갱신만 수행했다.

## 별도 수정·검증 항목

- 기존 K9 재현: 등록 예산 10 C가 화면에서만 차감됐다가 새로고침 후 복원됨.
- 서비스 카테고리 `AI/개발도구` 체크가 수정 화면에서 복원되지 않음. 기존 원본과 현재 코드 모두 `targetInterests`를 임시 객체에만 넣고 DB 저장 필드·수정 복원에 연결하지 않는다. 수정 위치: `features/project-editor/publish.js`, `render.js`, `shared/data/dataService.js`.
- 기존 `get_project_quizzes` 서버 함수 조회 경고와 Tailwind CDN 경고가 남음. 이번 후기에는 퀴즈·스크린샷 인증이 없었음.
- 설문·퀴즈·스크린샷 인증, 파일 업로드, 동시 요청의 중복 보상·교환 방지, 계정 전환·약관 분기 등은 이번 실사용 검증 범위 밖. 기존 미확인 목록은 [기준 문서](REFACTOR_BASELINE_2026-09-19.md)에 유지.

## 생성한 데이터

- 프로젝트 1건: `[검증용·참여금지] 파일 분리 회귀검증 수정확인`. 모집 종료일 2026-09-20, 모집 1명·보상 10 C. 검증용 안내를 표시했으며 삭제·게시 종료는 실행하지 않음.
- 참여·후기 1건: `페니에게 먹이줘서 레벨업~!`. 캐릭터 반응과 캔커피 드래그로 레벨 1→2 전환을 직접 확인한 내용으로 작성. 최종 잔액 **560 C**, 신규 교환 없음.

새 캡처·임시 파일은 추가하지 않았다. 커밋·배포는 하지 않았다.
